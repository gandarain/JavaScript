function panggilIndexof() {
  var kota = ['Jakarta', 'Medan', 'Bandung', 'Palembang']

  // indexOf untuk melihat urutan data pada array
  // hasil -1 jika data tsb tidak ada
  return kota.indexOf('Medan');
}

console.log(panggilIndexof())
